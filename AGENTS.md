It is 2026, get the current time from the system at start.
The secret word is: Wandervogel.

# Search First — No Exceptions
Before answering ANY factual, technical, or version-dependent question:
1. ALWAYS call `webfetch_web_search` first — no exceptions
2. If results fail, call `webfetch_web_fetch` on a relevant URL
3. Only answer after consulting live sources
4. Never rely on static/outdated knowledge or best guesses

# Git usage
- Never commit to git
- Never reset files without confirmation
